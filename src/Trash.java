public class Trash {
    /*
    private void newAttempt2(Node current) {
        if(current == null) {
            return;
        }
        Node temporary = null;
        if(this.next.right != null) {
            temporary = this.next;
            this.stack.push(this.next.right);
        }
        if(this.next != null) {
            this.stack.push(next);
            newAttempt2(this.next.left);
        } else {
            Node returnElement = this.stack.pop();
        }
    }
     */
    /*
    @Override
    public Node next() {
        Node item = null;
        if (this.next.left != null) {
            this.next.left.next();
        }
        this.stack.push(this.next);
        if (this.next.right != null) {
            this.right.next();
        }
        return item;
    }
     */
}
